# refman
The Embroidermodder Reference Manual (EMRM)

## Build

For example on Debian/Ubuntu this will build the reference manual:

```sh
git clone https://github.com/embroidermodder/refman
cd refman
sudo apt install texlive texlive-fonts-extra
bash build.sh
```
